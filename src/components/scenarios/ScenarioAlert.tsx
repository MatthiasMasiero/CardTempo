'use client';

export function ScenarioAlert({ children }: { children: React.ReactNode }) {
  return <div className="p-4 bg-white border border-stone-200 border-l-4 border-l-blue-500 rounded-lg">{children}</div>;
}
